{
  "name": "huemeld-app",
  "version": "1.0.0",
  "private": true,
  "description": "Huemeld — native (Capacitor) wrapper with AdMob + StoreKit purchases via RevenueCat",
  "scripts": {
    "sync-www": "node sync.mjs",
    "ios": "npm run sync-www && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor-community/admob": "^6.0.0",
    "@revenuecat/purchases-capacitor": "^9.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
